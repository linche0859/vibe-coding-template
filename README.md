# Vibe Coding HTML 編輯器

<div align="center">

一個專為課程活動設計的 HTML 模板編輯器，提供視覺化編輯介面與即時預覽功能，讓您輕鬆建立、管理並匯出課程活動頁面的 HTML 代碼。

![Vue.js](https://img.shields.io/badge/Vue.js-3.5-4FC08D?style=flat-square&logo=vue.js)
![TypeScript](https://img.shields.io/badge/TypeScript-5.9-3178C6?style=flat-square&logo=typescript)
![Vite](https://img.shields.io/badge/Vite-7.2-646CFF?style=flat-square&logo=vite)
![Tailwind CSS](https://img.shields.io/badge/Tailwind_CSS-4.1-38B2AC?style=flat-square&logo=tailwind-css)

</div>

---

## 📋 目錄

- [✨ 功能特色](#-功能特色)
- [🎯 專案簡介](#-專案簡介)
- [🛠️ 技術棧](#️-技術棧)
- [📦 安裝說明](#-安裝說明)
- [🚀 使用方式](#-使用方式)
- [📂 專案結構](#-專案結構)
- [🎨 核心功能詳解](#-核心功能詳解)
- [🔧 配置說明](#-配置說明)
- [📝 開發指南](#-開發指南)
- [🤝 貢獻指南](#-貢獻指南)
- [📄 授權條款](#-授權條款)

---

## ✨ 功能特色

### 🎨 視覺化編輯器

- **所見即所得**：即時預覽編輯結果，無需手動刷新
- **表單驗證**：自動驗證場次標籤格式（MM/DD）與報名連結的有效性
- **動態項目管理**：可新增、刪除、重新排序課程包括項目與活動資訊

### 📦 模板管理系統

- **多模板支援**：可建立、編輯、刪除、複製多個課程模板
- **本地儲存**：使用 `localStorage` 自動保存所有模板，重新開啟瀏覽器後資料不會丟失
- **預設模板**：內建預設模板，快速開始使用
- **智能命名**：自動產生不重複的模板名稱，避免命名衝突

### 🎯 一鍵匯出

- **HTML 代碼生成**：自動將表單資料轉換為標準化的 HTML 代碼
- **代碼複製**：一鍵複製生成的 HTML 至剪貼簿
- **安全性處理**：自動對用戶輸入進行 HTML 轉義，防止 XSS 攻擊
- **響應式設計**：生成的 HTML 自帶響應式樣式，適配各種螢幕尺寸

### 🌙 主題切換

- **深色/淺色模式**：支援深色與淺色主題自由切換
- **自動記憶**：系統記住用戶的主題偏好設定
- **平滑過渡**：主題切換時有流暢的動畫效果

### ♿ 無障礙支援

- **鍵盤導航**：完整支援鍵盤操作
- **ARIA 標籤**：遵循無障礙設計標準
- **語意化 HTML**：使用語意化標籤提升可讀性

---

## 🎯 專案簡介

Vibe Coding HTML 編輯器是一個專為線上課程活動設計的網頁應用程式，旨在簡化課程活動頁面的建立流程。傳統上，建立課程活動頁面需要手動編寫 HTML 代碼，不僅耗時且容易出錯。本專案提供了一個直覺的視覺化介面，讓使用者可以：

1. **快速建立**課程活動資訊表單
2. **即時預覽**頁面呈現效果
3. **輕鬆管理**多個課程模板
4. **一鍵匯出**標準化的 HTML 代碼

特別適合：

- 📚 線上教育平台運營人員
- 🎓 課程行銷人員
- 💼 活動策劃人員
- 👨‍💻 需要快速產生活動頁面的開發者

---

## 🛠️ 技術棧

### 前端框架與語言

- **[Vue 3](https://vuejs.org/)** `^3.5.24` - 漸進式 JavaScript 框架
  - 使用 `<script setup>` 語法糖
  - Composition API
  - 響應式系統
- **[TypeScript](https://www.typescriptlang.org/)** `~5.9.3` - 靜態類型檢查
  - 完整的型別定義
  - 介面與類型安全

### 建構工具

- **[Vite](https://vitejs.dev/)** `^7.2.4` - 下一代前端建構工具
  - 極速的熱模組替換 (HMR)
  - 優化的建構產出
  - 開箱即用的 TypeScript 支援

### UI 框架與樣式

- **[Tailwind CSS](https://tailwindcss.com/)** `^4.1.18` - 實用優先的 CSS 框架
  - 響應式設計
  - 深色模式支援
  - 自定義配置
- **[Radix Vue](https://www.radix-vue.com/)** `^1.9.17` - 無樣式的可訪問元件
  - Dialog、Button、Input 等元件
  - 完整的鍵盤導航
  - ARIA 標準支援
- **[tailwindcss-animate](https://github.com/jamiebuilds/tailwindcss-animate)** `^1.0.7` - 動畫工具類別

### 工具函式庫

- **[@vueuse/core](https://vueuse.org/)** `^14.1.0` - Vue Composition API 工具集
  - `useColorMode` - 主題切換
  - `useLocalStorage` - 本地儲存
  - 其他實用 composables
- **[class-variance-authority](https://cva.style/docs)** `^0.7.1` - CSS 變體管理
- **[clsx](https://github.com/lukeed/clsx)** `^2.1.1` + **[tailwind-merge](https://github.com/dcastil/tailwind-merge)** `^3.4.0` - 條件式樣式合併

### 開發工具

- **[ESLint](https://eslint.org/)** `^9.39.2` - 程式碼檢查
  - Vue 插件
  - TypeScript 支援
  - Prettier 整合
- **[Prettier](https://prettier.io/)** `^3.8.1` - 程式碼格式化
  - Tailwind CSS 類別排序
- **[vue-tsc](https://github.com/vuejs/language-tools)** `^3.1.4` - Vue TypeScript 編譯器

---

## 📦 安裝說明

### 環境需求

在開始之前，請確保您的開發環境符合以下要求：

- **Node.js**: `>= 18.0.0` (推薦使用 LTS 版本)
- **npm**: `>= 9.0.0` 或 **pnpm**: `>= 8.0.0` 或 **yarn**: `>= 1.22.0`
- **Git**: 用於版本控制

檢查版本：

```bash
node --version
npm --version
```

### 克隆專案

```bash
# 使用 HTTPS
git clone https://github.com/linche0859/vibe-coding-template.git

# 或使用 SSH
git clone git@github.com:linche0859/vibe-coding-template.git

# 進入專案目錄
cd vibe-coding-template
```

### 安裝依賴

```bash
# 使用 npm
npm install

# 或使用 pnpm (推薦，更快速)
pnpm install

# 或使用 yarn
yarn install
```

### 啟動開發伺服器

```bash
npm run dev
```

成功啟動後，在瀏覽器中開啟 [http://localhost:5173](http://localhost:5173) 即可看到應用程式。

---

## 🚀 使用方式

### 基本操作流程

#### 1. 建立新模板

- 應用程式會自動載入預設模板
- 預設模板包含範例資料，方便快速了解結構

#### 2. 編輯課程資訊

**頭部資訊**

- **標題**：例如「免費(限時限量)」
- **場次標籤**：格式必須為 `MM/DD`（例如 `03/03`）
- **副標題**：例如「名額有限，儘早加入」

**行動呼籲按鈕 (CTA)**

- **按鈕文字**：例如「立即報名」
- **報名連結**：必須是有效的 URL（自動驗證）
- **GA 追蹤標籤**：用於 Google Analytics 追蹤

**課程包括**

- **區塊標題**：例如「課程包括：」
- **課程項目**：可新增、刪除、重新排序
- 點擊「新增項目」按鈕可添加新的課程內容
- 點擊刪除按鈕 (紅色按鈕) 可移除項目

**活動資訊**

- **區塊標題**：例如「活動資訊：」
- **日期**：例如「03/03」
- **時間**：例如「7:00pm - 9:00pm」
- **上課方式**：例如「線上（報名成功後將透過 Line 與 Email 提供連結）」

#### 3. 即時預覽

- 左側為編輯表單，右側為即時預覽
- 任何修改都會立即反映在預覽區
- 預覽區完全模擬實際呈現效果

#### 4. 匯出 HTML 代碼

1. 點擊「匯出 HTML」按鈕
2. 系統會自動複製生成的 HTML 至剪貼簿
3. 顯示「HTML 已複製到剪貼簿」提示訊息
4. 將代碼貼至您的目標位置即可使用

#### 5. 管理模板

**複製模板**

- 點擊「複製」按鈕可快速建立相同內容的新模板
- 系統會自動產生不重複的模板名稱

**刪除模板**

- 點擊「刪除」按鈕
- 確認對話框會要求二次確認
- 刪除後無法復原，請謹慎操作

**重新命名模板**

- 點擊模板名稱即可編輯
- 支援使用日期或自定義名稱

### 快捷鍵 (計畫中)

| 快捷鍵         | 功能         |
| -------------- | ------------ |
| `Ctrl/Cmd + S` | 儲存當前模板 |
| `Ctrl/Cmd + E` | 匯出 HTML    |
| `Ctrl/Cmd + D` | 複製模板     |
| `Ctrl/Cmd + T` | 切換主題     |

---

## 📂 專案結構

```
vibe-coding-template/
├── public/                         # 靜態資源目錄
├── src/
│   ├── assets/                     # 專案資源（圖片、字體等）
│   ├── components/                 # Vue 元件目錄
│   │   ├── preview/                # 預覽相關元件
│   │   │   ├── Card.vue            # 預覽卡片容器
│   │   │   ├── CheckItem.vue       # 課程項目勾選項
│   │   │   ├── CTA.vue             # 行動呼籲按鈕預覽
│   │   │   ├── Header.vue          # 頭部資訊預覽
│   │   │   ├── Includes.vue        # 課程包括預覽
│   │   │   ├── Info.vue            # 活動資訊容器
│   │   │   └── InfoItem.vue        # 活動資訊項目
│   │   ├── ui/                     # 基礎 UI 元件（基於 Radix Vue）
│   │   │   ├── Button.vue          # 按鈕元件
│   │   │   ├── Dialog.vue          # 對話框元件
│   │   │   ├── DialogContent.vue   # 對話框內容
│   │   │   ├── DialogDescription.vue
│   │   │   ├── DialogFooter.vue
│   │   │   ├── DialogHeader.vue
│   │   │   ├── DialogTitle.vue
│   │   │   ├── Input.vue           # 輸入框元件
│   │   │   ├── Label.vue           # 標籤元件
│   │   │   └── index.ts            # UI 元件統一匯出
│   │   ├── ConfirmDialog.vue       # 確認對話框
│   │   ├── Editor.vue              # 主編輯器元件
│   │   ├── TemplateItem.vue        # 模板項目元件
│   │   └── ThemeToggle.vue         # 主題切換按鈕
│   ├── composables/                # Vue Composition API
│   │   ├── useTemplates.ts         # 模板管理邏輯
│   │   └── useTheme.ts             # 主題管理邏輯
│   ├── lib/
│   │   └── utils.ts                # 工具函式（cn 函式等）
│   ├── types/
│   │   └── course.ts               # TypeScript 類型定義
│   ├── utils/
│   │   ├── export.ts               # HTML 匯出邏輯
│   │   └── validation.ts           # 表單驗證邏輯
│   ├── App.vue                     # 根元件
│   ├── main.ts                     # 應用程式入口
│   └── style.css                   # 全域樣式
├── components.json                 # UI 元件配置
├── index.html                      # HTML 入口檔案
├── package.json                    # 專案依賴與腳本
├── postcss.config.js               # PostCSS 配置
├── tailwind.config.ts              # Tailwind CSS 配置
├── tsconfig.json                   # TypeScript 主配置
├── tsconfig.app.json               # TypeScript 應用配置
├── tsconfig.node.json              # TypeScript Node 配置
├── vite.config.ts                  # Vite 配置
└── README.md                       # 專案說明文件
```

### 核心檔案說明

#### `src/types/course.ts`

定義了專案的核心資料結構：

- `CourseData`: 課程資料介面
- `Template`: 模板介面

#### `src/composables/useTemplates.ts`

模板管理的核心邏輯：

- `loadTemplatesFromStorage()`: 從 localStorage 載入模板
- `saveTemplates()`: 儲存模板至 localStorage
- `addTemplate()`: 新增模板
- `deleteTemplate()`: 刪除模板
- `updateTemplate()`: 更新模板
- `duplicateTemplate()`: 複製模板

#### `src/utils/export.ts`

HTML 生成與匯出功能：

- `generateHTML()`: 將課程資料轉換為 HTML 字串
- `escapeHtml()`: HTML 特殊字元轉義

#### `src/utils/validation.ts`

表單驗證規則：

- 場次標籤格式驗證 (MM/DD)
- URL 有效性驗證

---

## 🎨 核心功能詳解

### 模板管理系統

#### 資料持久化

使用瀏覽器的 `localStorage` 實現資料持久化：

```typescript
// 自動監聽模板變化並儲存
watch(
  templates,
  (newTemplates) => {
    saveTemplates(newTemplates)
  },
  { deep: true }
)
```

#### 預設模板

應用程式啟動時會檢查 `localStorage`：

- 如果存在已保存的模板，則載入
- 如果不存在，則建立預設模板

### HTML 生成器

#### 安全性考量

所有用戶輸入都會經過 `escapeHtml()` 處理：

```typescript
function escapeHtml(text: string): string {
  const div = document.createElement('div')
  div.textContent = text
  return div.innerHTML
}
```

這可防止 XSS (跨站腳本) 攻擊。

#### 響應式設計

生成的 HTML 包含完整的響應式類別：

- 手機版 (預設)
- 平板版 (`lg:` 前綴)
- 桌面版 (自動適配)

### 表單驗證

#### 場次標籤驗證

```typescript
// 必須符合 MM/DD 格式
const regex = /^(0[1-9]|1[0-2])\/(0[1-9]|[12][0-9]|3[01])$/
```

#### URL 驗證

使用原生 `URL` 建構函式驗證：

```typescript
try {
  new URL(value)
  return true
} catch {
  return false
}
```

### 主題系統

使用 VueUse 的 `useColorMode` 實現：

- 自動檢測系統主題偏好
- 在 `localStorage` 中保存用戶選擇
- 即時切換不需重新載入頁面

---

## 🔧 配置說明

### Vite 配置 (`vite.config.ts`)

```typescript
export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
  },
})
```

- 設定 `@` 別名指向 `src` 目錄
- 啟用 Vue 插件

### Tailwind CSS 配置 (`tailwind.config.ts`)

自定義主題、顏色、動畫等設定，支援深色模式。

### TypeScript 配置

- `tsconfig.json`: 主配置
- `tsconfig.app.json`: 應用程式相關配置
- `tsconfig.node.json`: Node.js 環境配置

---

## 📝 開發指南

### 開發環境設定

#### 推薦的 IDE

- **[Visual Studio Code](https://code.visualstudio.com/)**
  - [Volar](https://marketplace.visualstudio.com/items?itemName=Vue.volar) - Vue 官方擴充功能
  - [TypeScript Vue Plugin (Volar)](https://marketplace.visualstudio.com/items?itemName=Vue.vscode-typescript-vue-plugin)
  - [ESLint](https://marketplace.visualstudio.com/items?itemName=dbaeumer.vscode-eslint)
  - [Prettier](https://marketplace.visualstudio.com/items?itemName=esbenp.prettier-vscode)
  - [Tailwind CSS IntelliSense](https://marketplace.visualstudio.com/items?itemName=bradlc.vscode-tailwindcss)

#### VS Code 設定

建議在 `.vscode/settings.json` 中添加：

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  }
}
```

### 可用的 npm 腳本

```bash
# 啟動開發伺服器（支援熱模組替換）
npm run dev

# 類型檢查與建構生產版本
npm run build

# 預覽建構後的應用程式
npm run preview
```

### 程式碼風格指南

#### Vue 元件

- 使用 `<script setup>` 語法
- 使用 Composition API
- Props 和 Emits 必須定義 TypeScript 類型

```vue
<script setup lang="ts">
import type { Template } from '@/types/course'

interface Props {
  template: Template
}

interface Emits {
  (e: 'update:template', template: Template): void
  (e: 'delete', id: string): void
}

const props = defineProps<Props>()
const emit = defineEmits<Emits>()
</script>
```

#### TypeScript

- 優先使用 `interface` 而非 `type`
- 明確定義函式回傳類型
- 避免使用 `any`

#### CSS / Tailwind

- 優先使用 Tailwind 工具類別
- 複雜元件可使用 `@apply` 指令
- 遵循移動優先的響應式設計

### 新增功能

#### 1. 新增資料欄位

**步驟 1**: 更新類型定義 (`src/types/course.ts`)

```typescript
export interface CourseData {
  // ...existing fields
  newField: {
    title: string
    content: string
  }
}
```

**步驟 2**: 更新預設資料 (`src/composables/useTemplates.ts`)

```typescript
function getDefaultCourseData(): CourseData {
  return {
    // ...existing fields
    newField: {
      title: '新欄位標題',
      content: '新欄位內容',
    },
  }
}
```

**步驟 3**: 新增編輯器元件 (`src/components/Editor.vue`)

**步驟 4**: 新增預覽元件 (`src/components/preview/`)

**步驟 5**: 更新 HTML 生成器 (`src/utils/export.ts`)

#### 2. 新增 UI 元件

使用 Radix Vue 元件庫：

```bash
# 參考 components.json 配置
# UI 元件放置於 src/components/ui/
```

### 除錯技巧

#### Vue DevTools

安裝 [Vue DevTools](https://devtools.vuejs.org/) 瀏覽器擴充功能：

- 檢查元件層級結構
- 查看響應式資料狀態
- 追蹤事件觸發

#### 瀏覽器 DevTools

- **Application → Local Storage**: 檢查保存的模板資料
- **Console**: 查看錯誤訊息與警告
- **Network**: 檢查資源載入狀態

---

## 🤝 貢獻指南

我們歡迎任何形式的貢獻！無論是回報錯誤、提出新功能建議、改善文件或提交程式碼。

### 貢獻流程

1. **Fork 本專案**

   ```bash
   # 點擊 GitHub 上的 Fork 按鈕
   ```

2. **建立功能分支**

   ```bash
   git checkout -b feature/amazing-feature
   ```

3. **提交您的更改**

   ```bash
   git add .
   git commit -m 'feat: 新增很棒的功能'
   ```

4. **推送至您的分支**

   ```bash
   git push origin feature/amazing-feature
   ```

5. **開啟 Pull Request**
   - 前往原始專案頁面
   - 點擊「New Pull Request」
   - 填寫詳細的 PR 描述

### Commit 訊息規範

遵循 [Conventional Commits](https://www.conventionalcommits.org/) 規範：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type 類型：**

- `feat`: 新功能
- `fix`: 錯誤修復
- `docs`: 文件更新
- `style`: 程式碼格式調整（不影響功能）
- `refactor`: 重構（不是新功能也不是修復）
- `perf`: 效能優化
- `test`: 測試相關
- `chore`: 建構流程或輔助工具變更

**範例：**

```
feat(editor): 新增課程分類選擇功能

- 新增分類下拉選單
- 更新類型定義
- 新增相應的預覽元件

Closes #123
```

### 程式碼審查標準

- ✅ 通過 ESLint 檢查
- ✅ 通過 TypeScript 類型檢查
- ✅ 遵循專案的程式碼風格
- ✅ 包含適當的註解
- ✅ 更新相關文件

---

## 📄 授權條款

本專案採用 MIT 授權條款 - 詳見 [LICENSE](LICENSE) 檔案

### MIT License 概要

您可以自由地：

- ✅ 使用本專案於商業或私人用途
- ✅ 修改原始碼
- ✅ 分發原始碼或修改後的版本
- ✅ 將本專案作為私有軟體使用

唯一條件：

- 📋 必須在您的專案中包含原始授權聲明與版權聲明

---

## 🙏 致謝

- [Vue.js](https://vuejs.org/) - 提供優秀的前端框架
- [Vite](https://vitejs.dev/) - 提供極速的開發體驗
- [Tailwind CSS](https://tailwindcss.com/) - 提供實用的 CSS 框架
- [Radix Vue](https://www.radix-vue.com/) - 提供無障礙的 UI 元件
- [VueUse](https://vueuse.org/) - 提供實用的 Composition API 工具

---

## 📞 聯絡資訊

- **專案維護者**: [linche0859](https://github.com/linche0859)
- **專案連結**: [https://github.com/linche0859/vibe-coding-template](https://github.com/linche0859/vibe-coding-template)
- **問題回報**: [GitHub Issues](https://github.com/linche0859/vibe-coding-template/issues)

---

## 🗺️ 未來規劃

### 計畫新增功能

- [ ] 模板匯入/匯出功能（JSON 格式）
- [ ] 拖拽排序課程項目
- [ ] 多語言支援（i18n）
- [ ] 更多預設模板選項
- [ ] 自定義顏色主題
- [ ] 歷史記錄功能（Undo/Redo）
- [ ] 雲端同步功能
- [ ] 模板分享功能
- [ ] Markdown 支援
- [ ] 圖片上傳功能

### 技術優化

- [ ] 單元測試（Vitest）
- [ ] E2E 測試（Playwright）
- [ ] 效能優化
- [ ] PWA 支援
- [ ] Docker 部署配置
- [ ] CI/CD 流程

---

<div align="center">

**如果這個專案對您有幫助，請給我們一個 ⭐️**

Made with ❤️ by [linche0859](https://github.com/linche0859)

</div>
